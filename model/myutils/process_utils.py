# Search the query for snip view
import multiprocessing


# def get_snip_view(func, query, embeddings, search_results):
#     p1 = multiprocessing.Process(target=func, args=(query, embeddings, search_results,))
#     if __name__ == "__main__":
#         print("Snip process start")
#         # creating processes
#
#         # starting process 1
#         p1.start()
#         # wait until process 1 is finished
#         print("Snip process end")
#     return p1
        # p1.join()
